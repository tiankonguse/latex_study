`只有一堆 n 个石子.`
`两个人轮流从这堆石子中取石子.`
`规定每次至少取一个，最多取 m 个`
`最后取光者得胜。`

`得出规律：n%(m+1)=0 时为 P 点，其他为 N 点`

`变形：减法游戏`
`只有一堆 n 个石子`
`两个人轮流从这堆石子中取物`
`规定每次取的个数只能为一集合中的元素`
`最后取光者得胜。`

`未变形的实际上是一个 1 到 n 的集合。`
`例如：集合是 2 的幂次（即：1，2，4，8，16…）`
`得出规律：n%3=0 为 P 点`
